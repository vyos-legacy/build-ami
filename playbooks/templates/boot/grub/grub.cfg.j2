set default=0
set timeout=0

menuentry "VyOS AMI (HVM) {{ version_string.stdout }}" {
  linux /boot/{{ version_string.stdout }}/vmlinuz boot=live quiet vyatta-union=/boot/{{ version_string.stdout }} console=ttyS0
  initrd /boot/{{ version_string.stdout }}/initrd.img
}
